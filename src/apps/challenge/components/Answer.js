import React from 'react'

import { withStyles } from '@material-ui/core'

const styles = theme => ({})

const Answer = ({ classes, question }) => (
  <div> SOON, you will be able to play a challenge here! </div>
)

export default withStyles(styles)(Answer)
